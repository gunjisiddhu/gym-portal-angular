import {TrainerRegistration} from '../models/trainer-registration';

describe('TrainerRegistration', () => {
  it('should create an instance', () => {
    expect(new TrainerRegistration()).toBeTruthy();
  });
});
